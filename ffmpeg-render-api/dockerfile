FROM node:20-bullseye

RUN apt-get update && apt-get install -y ffmpeg && rm -rf /var/lib/apt/lists/*

WORKDIR /app
COPY ffmpeg-render-api/package.json ffmpeg-render-api/package-lock.json* ./
RUN npm install

COPY . .
EXPOSE 3000

CMD ["node", "ffmpeg-render-api/server.js"]
